export const LABEL_TYPES = Object.freeze({
  ROUND: 'round',
  SQUARE: 'square',
});

export const LABEL_COLORS = Object.freeze({
  DEFAULT: 'default',
  NEW: 'new',
  NORMAL: 'normal',
  URGENT: 'urgent',
  LOW: 'low',
  HiGH: 'high',
});
